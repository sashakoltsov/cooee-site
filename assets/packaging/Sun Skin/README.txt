Drop the 6 face files here, named exactly:
  front  back  left  right  top  bottom
e.g. front.svg / front.png / front.jpg

SVG is best (crisp at any zoom; outline the type, no embedded rasters).
Then set this product's `ext` in index.html to 'svg' (or 'png' / 'jpg') to match.

Optional extra for the open-box state: top-fold (the lid flap), same format.

Until the files are present the box shows a flat placeholder.
If the box proportions differ from Aperitivo, edit this product's `ratios` in index.html
(ratios = face-image width/height and depth-face width/height).
